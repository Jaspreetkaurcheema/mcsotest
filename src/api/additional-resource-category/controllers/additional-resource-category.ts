/**
 * additional-resource-category controller
 */

import { factories } from "@strapi/strapi";

export default factories.createCoreController(
  "api::additional-resource-category.additional-resource-category"
);
